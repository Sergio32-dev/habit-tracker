# 🔗 Пошаговая интеграция БД в проект

## ✅ Что уже сделано:

1. ✅ База данных создана в SQL Server
2. ✅ API endpoints созданы (`api/auth/register.js`, `api/auth/login.js`, `api/admin/users.js`)
3. ✅ Админ-панель с просмотром пользователей готова

## ⚠️ Что нужно сделать:

### Шаг 1: Установить зависимости

```bash
cd C:\Users\smama\Desktop\IT
npm install mssql bcrypt dotenv
```

### Шаг 2: Проверить API endpoints

API endpoints используют CommonJS (`module.exports`), что правильно для Vercel.

### Шаг 3: Обновить AuthContext (опционально)

Пока можно оставить AuthContext с localStorage для разработки.
API endpoints готовы, но можно сначала протестировать их напрямую.

### Шаг 4: Настроить переменные окружения

Для локальной разработки создайте `.env` файл (не коммитьте в Git!):

```env
DB_USER=sa
DB_PASSWORD=ваш_пароль
DB_SERVER=localhost
DB_DATABASE=HabitTracker
DB_ENCRYPT=false
DB_TRUST_CERT=true
```

### Шаг 5: Тестирование API

Используйте Postman или curl для тестирования API endpoints:

**Регистрация:**
```bash
curl -X POST http://localhost:5173/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","email":"test@test.com","password":"test123"}'
```

---

## 🔍 Диагностика ошибки "Unexpected token 'c'"

Эта ошибка означает, что API возвращает JavaScript код вместо JSON.

**Возможные причины:**
1. Зависимости не установлены (`mssql`, `bcrypt`)
2. API endpoint не найден (404), возвращается HTML
3. Ошибка в коде API endpoint

**Решение:**
1. Установите зависимости: `npm install mssql bcrypt dotenv`
2. Проверьте что файлы в `api/` используют `module.exports`
3. Для локальной разработки используйте Vercel CLI: `npx vercel dev`

---

**См. также:** `DB_INTEGRATION_GUIDE.md` для подробной инструкции


