# 🔧 Решение проблемы с PowerShell

## Проблема
```
npm : Невозможно загрузить файл... так как выполнение сценариев отключено в этой системе
```

## ✅ Решение 1: Использовать готовые .bat файлы (САМЫЙ ПРОСТОЙ)

Я создал для вас два файла:
- **RUN.bat** - для установки зависимостей
- **START.bat** - для запуска приложения

Просто дважды кликните на них!

---

## ✅ Решение 2: Изменить политику выполнения PowerShell

### Вариант А: Для текущего пользователя (рекомендуется)

1. Откройте PowerShell **от имени администратора**:
   - Нажмите `Win + X`
   - Выберите "Windows PowerShell (Администратор)" или "Терминал (Администратор)"

2. Выполните команду:
   ```powershell
   Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
   ```

3. Нажмите `Y` для подтверждения

4. Перезапустите терминал в VS Code

### Вариант Б: Для текущей сессии (временно)

В терминале VS Code выполните:
```powershell
Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process
```

Это работает только для текущей сессии терминала.

---

## ✅ Решение 3: Использовать CMD вместо PowerShell

В VS Code:

1. Нажмите `Ctrl + Shift + P`
2. Введите: `Terminal: Select Default Profile`
3. Выберите **Command Prompt** (не PowerShell)

Или в настройках VS Code добавьте:
```json
{
  "terminal.integrated.defaultProfile.windows": "Command Prompt"
}
```

---

## ✅ Решение 4: Использовать npm через cmd напрямую

В терминале PowerShell выполните:
```powershell
cmd /c "cd C:\Users\smama\Desktop\IT && npm install"
```

Или:
```powershell
cmd /c npm install
```

---

## 🎯 Рекомендация

**Самый простой способ:** Используйте файлы **RUN.bat** и **START.bat**, которые я создал. Просто дважды кликните на них!

Или измените политику выполнения один раз (Решение 2, Вариант А), и больше не будет проблем.

---

**После решения проблемы, откройте проект в VS Code и запустите `npm install`!**

