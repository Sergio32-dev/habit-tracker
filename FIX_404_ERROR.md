# 🔧 Исправление ошибки 404

## Проблема

Ошибка 404 означает, что Vercel не находит endpoint `/api/yookassa/create-payment`.

## Решения

### 1. Проверьте структуру папок

Убедитесь, что файл находится по пути:
```
api/
  └── yookassa/
      └── create-payment.js
```

### 2. Изменил формат на CommonJS

Vercel лучше работает с CommonJS форматом (`module.exports`) вместо ES modules (`export default`).

Файл уже обновлен на `module.exports`.

### 3. Убедитесь, что файлы загружены на Vercel

После обновления кода:
```bash
cd C:\Users\smama\Desktop\IT
git add .
git commit -m "Исправлен формат API endpoint"
git push
```

Или через Vercel CLI:
```bash
vercel --prod
```

### 4. Проверьте, что Vercel видит функции

После деплоя:
1. Откройте Vercel Dashboard
2. Перейдите в **Functions** вкладку
3. Должна быть функция `api/yookassa/create-payment`

### 5. Тест простого endpoint

Создан тестовый файл `api/test.js`. 

Попробуйте открыть в браузере:
```
https://your-app.vercel.app/api/test
```

Должен вернуться JSON с сообщением "API работает!".

Если этот endpoint работает, значит Serverless Functions настроены правильно.

---

## Альтернативное решение: Использовать Netlify

Если Vercel Functions не работают, можно использовать Netlify Functions:

1. Создайте папку `netlify/functions/`
2. Переместите туда функцию
3. Используйте формат Netlify Functions

Но сначала попробуйте обновленный код на Vercel!

---

## Быстрая проверка

1. Убедитесь, что файл `api/yookassa/create-payment.js` существует
2. Пересоберите и загрузите на Vercel
3. Проверьте `/api/test` - должен работать
4. Если `/api/test` работает, но `/api/yookassa/create-payment` нет - проблема в файле

---

**После обновления попробуйте снова!**


