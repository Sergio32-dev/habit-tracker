# 🎯 Настройка проекта в Visual Studio Code

## 📋 Быстрый старт

### Шаг 1: Откройте проект в Visual Studio Code

1. Запустите **Visual Studio Code**
2. Нажмите `File → Open Folder...` (или `Ctrl + K, Ctrl + O`)
3. Выберите папку: `C:\Users\smama\Desktop\IT`
4. Нажмите "Выбрать папку"

### Шаг 2: Установите зависимости (если еще не установлены)

1. Откройте терминал в VS Code: `Ctrl + ~` (тильда)
2. Убедитесь, что вы в правильной папке
3. Выполните:
   ```powershell
   npm install
   ```

### Шаг 3: Запустите приложение

**Способ 1: Через терминал (самый простой)**
1. Откройте терминал: `Ctrl + ~`
2. Выполните:
   ```powershell
   npm run dev
   ```
3. В терминале появится адрес (обычно `http://localhost:5173`)
4. Нажмите `Ctrl + Click` на адрес или скопируйте его в браузер

**Способ 2: Через меню запуска (F5)**
1. Нажмите `F5` или `Ctrl + F5`
2. Выберите "Запустить приложение (Dev)"
3. Приложение запустится в терминале
4. Откройте браузер и перейдите на `http://localhost:5173`

**Способ 3: Через панель задач**
1. Нажмите `Ctrl + Shift + P`
2. Введите: `Tasks: Run Task`
3. Выберите `npm: dev`
4. Приложение запустится

---

## 🛠 Полезные команды

### В терминале VS Code:

```powershell
# Установить зависимости
npm install

# Запустить в режиме разработки
npm run dev

# Собрать для продакшена
npm run build

# Просмотреть собранное приложение
npm run preview
```

---

## 🎨 Рекомендуемые расширения

VS Code предложит установить рекомендуемые расширения при открытии проекта. Или установите вручную:

1. **ESLint** - проверка кода
2. **Prettier** - форматирование кода
3. **Tailwind CSS IntelliSense** - автодополнение для CSS
4. **TypeScript и JavaScript Language Features** - поддержка JS/TS

---

## 📁 Структура проекта

```
IT/
├── src/                    # Исходный код приложения
│   ├── components/        # React компоненты
│   ├── App.jsx           # Главный компонент
│   └── main.jsx          # Точка входа
├── .vscode/              # Настройки VS Code
│   ├── launch.json      # Конфигурация запуска
│   ├── tasks.json       # Задачи
│   └── settings.json    # Настройки редактора
├── package.json         # Зависимости проекта
├── vite.config.js      # Конфигурация Vite
└── index.html          # HTML шаблон
```

---

## 🚀 Горячие клавиши

- `Ctrl + ~` - открыть/закрыть терминал
- `F5` - запустить отладку
- `Ctrl + Shift + P` - палитра команд
- `Ctrl + B` - скрыть/показать боковую панель
- `Ctrl + /` - закомментировать строку

---

## 🔧 Настройка терминала

Если Node.js не работает в терминале VS Code:

1. Перезапустите VS Code полностью
2. Или добавьте Node.js в PATH системы:
   - `Win + R` → `sysdm.cpl`
   - Вкладка "Дополнительно" → "Переменные среды"
   - Добавьте `C:\Program Files\nodejs` в PATH

---

## 📱 Открытие сайта в браузере

После запуска `npm run dev`:

1. В терминале появится адрес: `http://localhost:5173`
2. **Способ 1:** Нажмите `Ctrl + Click` на адрес в терминале
3. **Способ 2:** Скопируйте адрес и вставьте в браузер
4. **Способ 3:** Просто введите в адресной строке браузера: `localhost:5173`

---

## ✅ Проверка работы

После открытия сайта в браузере вы должны увидеть:
- ✅ Заголовок "Мои Привычки"
- ✅ Статистику (если есть привычки)
- ✅ Список привычек
- ✅ Кнопку "+" внизу справа для добавления привычек

---

## 🐛 Решение проблем

### Порт 5173 занят
```powershell
# Найдите процесс, использующий порт
netstat -ano | findstr :5173

# Или измените порт в vite.config.js
```

### Node.js не найден
- Перезапустите VS Code
- Проверьте PATH: `$env:PATH` в терминале
- Переустановите Node.js с галочкой "Add to PATH"

### Зависимости не устанавливаются
```powershell
# Очистите кэш и переустановите
npm cache clean --force
npm install
```

---

**Готово! Теперь вы можете работать с проектом прямо в Visual Studio Code! 🎉**


