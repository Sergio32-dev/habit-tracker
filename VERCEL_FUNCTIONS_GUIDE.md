# 🔍 Где посмотреть Functions на Vercel

## ❌ Где НЕ искать

**Settings → Functions** - это только настройки (регион, CPU, память), а не список функций.

## ✅ Где смотреть Functions

### Способ 1: Через Deployments (рекомендуется)

1. Откройте проект на Vercel
2. Перейдите во вкладку **"Deployments"** (слева в меню)
3. Выберите **последний deployment** (самый верхний)
4. В карточке deployment нажмите на **"View Function Logs"** или **"View Build Logs"**
5. Или прокрутите вниз до секции **"Functions"** - там будет список всех функций

### Способ 2: Через конкретный deployment

1. **Deployments** → выберите deployment
2. Откройте детали deployment
3. Найдите секцию **"Functions"** или **"Serverless Functions"**
4. Там будут перечислены все функции, например:
   - `/api/test`
   - `/api/yookassa/create-payment`

### Способ 3: Через URL напрямую

Просто откройте в браузере URL вашего сайта + путь к функции:

```
https://your-app.vercel.app/api/test
```

Если видите JSON ответ - функция работает!
Если 404 - функция не найдена.

---

## 📋 Что должно быть видно

После успешного деплоя должны быть функции:

1. **`/api/test`** - тестовая функция
2. **`/api/yookassa/create-payment`** - функция для оплаты

---

## 🐛 Если функции не видны

### Причина 1: Файлы не попали в деплой

Проверьте:
- Файл `api/yookassa/create-payment.js` существует в проекте?
- Файлы добавлены в Git (если используете Git)?
- После `git push` Vercel пересобрал проект?

### Причина 2: Ошибка при сборке

1. Перейдите в **Deployments**
2. Выберите последний deployment
3. Посмотрите **Build Logs** - там могут быть ошибки

### Причина 3: Неправильный формат файла

Убедитесь, что файл использует CommonJS:
```javascript
module.exports = async function handler(req, res) {
  // ...
};
```

---

## 🧪 Быстрая проверка

1. Откройте: `https://your-app.vercel.app/api/test`
2. Должен вернуться JSON: `{"success": true, "message": "API работает!"}`
3. Если работает - значит функции настроены правильно
4. Если 404 - функции не созданы

---

## 📸 Где искать в интерфейсе

```
Vercel Dashboard
  └── Ваш проект
      └── Deployments (вкладка вверху)
          └── Последний deployment
              └── Секция "Functions" (внизу страницы)
                  ├── /api/test
                  └── /api/yookassa/create-payment
```

---

**Главное: Functions видны в Deployments, а не в Settings! 📁**

