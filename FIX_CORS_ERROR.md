# 🔧 Исправление ошибки "Failed to fetch"

## Проблема

Ошибка "Failed to fetch" возникает потому, что браузер блокирует прямые запросы к API ЮKassa из-за CORS (Cross-Origin Resource Sharing).

## Решение

Создан backend endpoint `/api/yookassa/create-payment` который будет работать на Vercel как Serverless Function.

---

## Что было сделано

1. ✅ Создан файл `api/yookassa/create-payment.js` - backend endpoint
2. ✅ Обновлен `yookassaService.js` - теперь использует backend endpoint
3. ✅ Обновлен `vercel.json` - настройки для Serverless Functions

---

## Как обновить на Vercel

### Шаг 1: Соберите проект

```bash
cd C:\Users\smama\Desktop\IT
npm run build
```

### Шаг 2: Загрузите на Vercel

**Вариант А: Через CLI**

```bash
vercel --prod
```

**Вариант Б: Через Git**

```bash
git add .
git commit -m "Добавлен backend endpoint для ЮKassa"
git push
```

**Вариант В: Через веб-интерфейс**

1. Откройте проект на Vercel
2. Deployments → последний deployment → Redeploy

---

## Как это работает

1. **Frontend** отправляет запрос на `/api/yookassa/create-payment`
2. **Backend** (Serverless Function на Vercel) получает запрос
3. **Backend** делает запрос к API ЮKassa с секретным ключом
4. **Backend** возвращает URL для оплаты
5. **Frontend** перенаправляет пользователя на страницу оплаты

---

## Проверка работы

После обновления на Vercel:

1. Откройте сайт
2. Попробуйте оплату
3. Ошибка "Failed to fetch" должна исчезнуть
4. Вы будете перенаправлены на страницу оплаты ЮKassa

---

## Если все еще не работает

1. **Проверьте логи Vercel:**
   - Dashboard → Deployments → выберите последний
   - Посмотрите Function Logs

2. **Проверьте консоль браузера:**
   - Откройте DevTools (F12)
   - Вкладка Console - там могут быть дополнительные ошибки

3. **Убедитесь, что файл на месте:**
   - `api/yookassa/create-payment.js` должен существовать

---

**После обновления все должно заработать! 🎉**


