# 🗄️ Полная настройка SQL Server для проекта

## 📋 Оглавление

1. [Установка SQL Server](#установка-sql-server)
2. [Создание базы данных](#создание-базы-данных)
3. [Настройка проекта](#настройка-проекта)
4. [Настройка Vercel](#настройка-vercel)
5. [Тестирование](#тестирование)
6. [Интеграция с фронтендом](#интеграция-с-фронтендом)

---

## 1. Установка SQL Server

### Шаг 1: Скачайте SQL Server 2025 Developer Edition

1. Перейдите на: https://www.microsoft.com/sql-server/sql-server-downloads
2. Выберите **"SQL Server 2025 Developer Edition"**
3. Нажмите **"Download now"**
4. Сохраните установщик

### Шаг 2: Установите SQL Server

1. Запустите установщик
2. Выберите **"Basic"** для быстрой установки (или **"Custom"** для настройки)
3. **ВАЖНО:** Запомните пароль для учетной записи **SA** (системный администратор)
4. Дождитесь завершения установки

### Шаг 3: Установите SQL Server Management Studio (SSMS)

1. Скачайте SSMS: https://aka.ms/ssmsfullsetup
2. Установите для управления базой данных

---

## 2. Создание базы данных

### Шаг 1: Подключитесь к SQL Server через SSMS

1. Откройте **SQL Server Management Studio**
2. В диалоге подключения:
   - **Server name:** `localhost` или `(local)`
   - **Authentication:** SQL Server Authentication
   - **Login:** `sa`
   - **Password:** ваш пароль SA
   - **ВАЖНО:** ✅ Установите галочку **"Доверенный сертификат сервера"** (Trust server certificate)
   - **Шифрование:** можно оставить "Обязательно" или изменить на "Опционально"
3. Нажмите **"Connect"**

**Если ошибка подключения:** См. `SQL_CONNECTION_FIX.md`

### Шаг 2: Выполните SQL скрипт

1. Откройте файл `database/create_database.sql`
2. Скопируйте весь код
3. В SSMS: **File → New → Query with Current Connection**
4. Вставьте код
5. Нажмите **F5** или кнопку **Execute**
6. Убедитесь что все команды выполнены успешно

**Результат:** База данных `HabitTracker` создана с таблицами:
- `users` - пользователи
- `habits` - привычки
- `habit_completions` - выполненные задачи
- `subscriptions` - подписки

---

## 3. Настройка проекта

### Шаг 1: Установите зависимости

```bash
cd C:\Users\smama\Desktop\IT
npm install mssql bcrypt dotenv
```

### Шаг 2: Создайте конфигурационный файл

1. Скопируйте `database/config.example.js` как `database/config.js`
2. Откройте `database/config.js`
3. Заполните данные:

```javascript
module.exports = {
  user: 'sa',
  password: 'ВАШ_ПАРОЛЬ_SA',
  server: 'localhost',
  database: 'HabitTracker',
  options: {
    encrypt: false,
    trustServerCertificate: true,
    enableArithAbort: true
  }
};
```

### Шаг 3: Тестируйте подключение

```bash
cd database
npm install
node scripts/test-connection.js
```

Если видите "✅ Подключение успешно!" - все настроено правильно!

---

## 4. Настройка Vercel

### Шаг 1: Добавьте переменные окружения

В Vercel Dashboard → Project Settings → Environment Variables:

```
DB_USER=sa
DB_PASSWORD=ваш_пароль
DB_SERVER=ваш_сервер (для облачного SQL Server)
DB_DATABASE=HabitTracker
DB_ENCRYPT=false
DB_TRUST_CERT=true
```

**⚠️ ВАЖНО:** Для продакшена используйте облачный SQL Server (Azure SQL Database) или VPS!

### Шаг 2: Обновите API endpoints

Все API endpoints уже созданы:
- `/api/auth/register` - регистрация
- `/api/auth/login` - вход
- `/api/admin/users` - список пользователей (для админов)

---

## 5. Создание первых пользователей

### Вариант 1: Через API регистрации

После запуска API можно зарегистрировать пользователей через фронтенд.

### Вариант 2: Через SQL скрипт (для админа)

В SSMS выполните:

```sql
USE HabitTracker;

-- Генерируйте хеш через Node.js:
-- const bcrypt = require('bcrypt');
-- const hash = await bcrypt.hash('admin123', 10);
-- console.log(hash);

-- Затем вставьте хеш в SQL:
UPDATE users 
SET password_hash = 'ВАШ_BCRYPT_ХЕШ'
WHERE username = 'admin';
```

---

## 6. Интеграция с фронтендом

См. файл `FRONTEND_DB_INTEGRATION.md` для инструкций по обновлению фронтенда.

---

## 📁 Структура файлов

```
database/
  ├── create_database.sql      # SQL скрипт создания БД
  ├── config.example.js        # Пример конфигурации
  ├── config.js                # Ваша конфигурация (не коммитьте в Git!)
  ├── package.json             # Зависимости для БД
  └── scripts/
      └── test-connection.js   # Тест подключения

api/
  ├── db/
  │   └── connection.js        # Подключение к БД
  ├── auth/
  │   ├── register.js          # Регистрация
  │   └── login.js             # Вход
  └── admin/
      └── users.js             # Список пользователей
```

---

## ✅ Чеклист

- [ ] SQL Server установлен
- [ ] SSMS установлен
- [ ] База данных создана (выполнен create_database.sql)
- [ ] Зависимости установлены (mssql, bcrypt, dotenv)
- [ ] config.js создан и заполнен
- [ ] Тест подключения успешен
- [ ] Переменные окружения добавлены в Vercel (для продакшена)
- [ ] API endpoints протестированы

---

**Готово! База данных настроена! 🎉**

