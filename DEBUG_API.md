# 🐛 Отладка API endpoint

## Проблема: "Unexpected end of JSON input"

Эта ошибка означает, что сервер возвращает пустой ответ или не валидный JSON.

## Решения

### 1. Проверьте логи Vercel

После деплоя на Vercel:
1. Откройте Dashboard Vercel
2. Перейдите в **Deployments**
3. Выберите последний deployment
4. Откройте **Function Logs**
5. Посмотрите ошибки там

### 2. Проверьте локально (если используете Vercel CLI)

Запустите локальный сервер:
```bash
vercel dev
```

Это запустит Serverless Functions локально на `http://localhost:3000`

### 3. Проверьте формат ответа

Backend endpoint должен возвращать JSON с полем `success`:
```json
{
  "success": true,
  "paymentUrl": "...",
  "paymentId": "..."
}
```

### 4. Проверьте CORS

Убедитесь, что в коде есть обработка CORS:
```javascript
res.setHeader('Access-Control-Allow-Origin', '*');
```

### 5. Временное решение: Логирование

Если проблема сохраняется, можно добавить больше логов:

В `api/yookassa/create-payment.js`:
```javascript
console.log('Request body:', req.body);
console.log('Request headers:', req.headers);
```

Затем проверьте логи в Vercel Dashboard.

---

## Альтернативное решение: Использовать другой хостинг

Если Vercel Serverless Functions не работают, можно:

1. **Использовать Netlify Functions**
2. **Использовать Railway/Render** (выделенный сервер)
3. **Использовать Firebase Cloud Functions**

---

## Быстрая проверка

Попробуйте простой тест - создайте файл `api/test.js`:

```javascript
export default function handler(req, res) {
  res.status(200).json({ 
    success: true, 
    message: 'API работает!' 
  });
}
```

Затем откройте `/api/test` в браузере - должен вернуться JSON.

---

**После обновления кода, пересоберите и загрузите на Vercel!**


