# 🔧 Исправление ошибки подключения к SQL Server

## ❌ Проблема

Ошибка: "Цепочка сертификатов выпущена центром сертификации, не имеющим доверия"

## ✅ Решение

### Вариант 1: Включить "Доверенный сертификат сервера" (РЕКОМЕНДУЕТСЯ)

1. В окне подключения SSMS найдите чекбокс:
   - **"Доверенный сертификат сервера"** (Trust server certificate)

2. **УСТАНОВИТЕ ГАЛОЧКУ** ✅

3. Нажмите **"Подключиться"** (Connect)

**Это безопасно для локального SQL Server!**

---

### Вариант 2: Изменить настройки шифрования

Если вариант 1 не помог:

1. В поле **"Шифрование"** (Encryption) выберите:
   - **"Опционально"** (Optional) вместо "Обязательно" (Required)

2. Установите галочку **"Доверенный сертификат сервера"**

3. Нажмите **"Подключиться"**

---

## 📝 Настройки подключения для локального сервера:

- **Имя сервера:** `localhost` ✅
- **Проверка подлинности:** SQL Server Authentication ✅
- **Имя пользователя:** `sa` ✅
- **Пароль:** ваш пароль ✅
- **База данных:** оставьте по умолчанию (можно выбрать `HabitTracker` после создания)
- **Шифрование:** Опционально или Обязательно (если установлена галочка доверия)
- **Доверенный сертификат сервера:** ✅ **ОБЯЗАТЕЛЬНО ВКЛЮЧИТЬ!**

---

## ✅ Проверка успешного подключения

После подключения вы должны увидеть в SSMS:
- В левой панели: "Базы данных" (Databases)
- Можете открыть "New Query" для выполнения SQL команд

---

**После успешного подключения выполните `database/create_database.sql` для создания базы данных!**


