# 🔄 Как обновить сайт на Vercel

## Быстрый способ (через командную строку)

### Шаг 1: Соберите проект

```bash
cd C:\Users\smama\Desktop\IT
npm run build
```

### Шаг 2: Обновите на Vercel

**Вариант А: Через Vercel CLI (рекомендуется)**

```bash
vercel --prod
```

Если еще не авторизованы:
```bash
vercel login
```

**Вариант Б: Через Git (если используете GitHub)**

1. Закоммитьте изменения:
   ```bash
   git add .
   git commit -m "Добавлена интеграция с ЮKassa"
   git push
   ```

2. Vercel автоматически пересоберет проект при push в репозиторий

---

## Через веб-интерфейс Vercel

1. Откройте ваш проект на https://vercel.com
2. Перейдите в **Deployments**
3. Нажмите на последний deployment (три точки)
4. Выберите **Redeploy**
5. Нажмите **Redeploy** еще раз

---

## Проверка обновления

После деплоя:
1. Подождите 1-2 минуты (время сборки)
2. Откройте ваш сайт (ссылка из Vercel)
3. Проверьте, что изменения применились
4. Можно обновить страницу с очисткой кэша: `Ctrl + Shift + R` (Windows) или `Cmd + Shift + R` (Mac)

---

## Если изменения не появились

1. **Очистите кэш браузера:**
   - `Ctrl + Shift + Delete` → очистить кэш
   - Или откройте в режиме инкогнито

2. **Проверьте логи сборки:**
   - В Vercel Dashboard → Deployments → выберите последний
   - Посмотрите Build Logs на ошибки

3. **Проверьте, что код сохранен:**
   - Убедитесь, что все файлы сохранены
   - Проверьте, что `npm run build` работает локально

---

**После обновления ваш сайт будет использовать тестовые ключи ЮKassa! 🎉**

