# 🔧 Устранение проблем с подключением к SQL Server

## ❌ Ошибка: "Цепочка сертификатов выпущена центром сертификации..."

### Причина
SQL Server использует самоподписанный сертификат для локального подключения, который не доверен по умолчанию.

### ✅ Решение

**В SSMS при подключении:**

1. Найдите секцию **"Доверенный сертификат сервера"** (Trust server certificate)
2. **Установите галочку** ✅
3. Нажмите **"Подключиться"**

Это безопасно для локального SQL Server!

---

## ❌ Ошибка: "Не удалось найти SQL Server"

### Решения:

1. **Проверьте, что SQL Server запущен:**
   - Откройте **"Службы"** (Services)
   - Найдите **"SQL Server (MSSQLSERVER)"** или **"SQL Server (SQLEXPRESS)"**
   - Убедитесь что статус: **"Выполняется"** (Running)

2. **Попробуйте другой сервер:**
   - Вместо `localhost` используйте: `.\SQLEXPRESS` или `(local)\SQLEXPRESS`
   - Или IP адрес: `127.0.0.1`

3. **Включите SQL Server Browser:**
   - В "Службы" найдите **"SQL Server Browser"**
   - Запустите службу

---

## ❌ Ошибка: "Неверный логин или пароль"

### Решения:

1. **Проверьте пароль SA:**
   - Используйте тот пароль, который указали при установке SQL Server

2. **Сброс пароля SA (если забыли):**
   - Откройте **"Конфигурация SQL Server"** (SQL Server Configuration Manager)
   - Перейдите в **"SQL Server Services"**
   - Остановите SQL Server
   - Запустите SQL Server в однопользовательском режиме с флагом `-m`
   - Подключитесь через командную строку и сбросьте пароль

3. **Включите SQL Server Authentication:**
   - В SSMS: правый клик на сервере → Properties → Security
   - Выберите **"SQL Server and Windows Authentication mode"**
   - Перезапустите SQL Server

---

## ❌ Ошибка: "Порт 1433 недоступен"

### Решения:

1. **Проверьте брандмауэр:**
   - Разрешите SQL Server в брандмауэре Windows
   - Или временно отключите брандмауэр для тестирования

2. **Проверьте порт SQL Server:**
   - По умолчанию: `1433`
   - В "Конфигурация SQL Server" → "SQL Server Network Configuration" → "Protocols for MSSQLSERVER" → TCP/IP → Properties → IP Addresses

---

## ✅ Проверка успешного подключения

После подключения вы должны увидеть:

1. В левой панели SSMS:
   - Имя вашего сервера (например, `(localdb)\MSSQLLocalDB`)
   - Папка **"Databases"** (Базы данных)
   - Папка **"Security"** (Безопасность)

2. Можете выполнить тестовый запрос:
   ```sql
   SELECT @@VERSION;
   ```
   Должна вернуться версия SQL Server.

---

## 📞 Дополнительная помощь

Если проблемы продолжаются:
1. Проверьте логи SQL Server в Event Viewer
2. Убедитесь что SQL Server установлен правильно
3. Попробуйте переустановить SQL Server с настройками по умолчанию


