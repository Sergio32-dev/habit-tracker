# ✅ Исправлен vercel.json

## Проблема

Ошибка: "Function Runtimes must have a valid version"

Причина: В `vercel.json` была неправильная секция `functions` с настройками runtime.

## Решение

Упростил `vercel.json` - убрал секцию `functions`, потому что Vercel автоматически распознает функции в папке `api/`.

## Что изменилось

**Было:**
```json
{
  "functions": {
    "api/**/*.js": {
      "runtime": "nodejs18.x"
    }
  }
}
```

**Стало:**
```json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite"
}
```

Vercel автоматически:
- ✅ Распознает функции в папке `api/`
- ✅ Использует Node.js runtime по умолчанию
- ✅ Не нужно указывать runtime вручную

---

## Что дальше

1. ✅ Изменения закоммичены в Git
2. Сделайте `git push` (если уже подключен к GitHub)
3. Или загрузите через `vercel --prod`

После этого ошибка должна исчезнуть! 🎉


